using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PickupPlayerManager : MonoBehaviour
{
    public float finalPartPickupAmount;

    void Update()
    {
        if(finalPartPickupAmount == 1)
		{
           // "can escape"
		}
    }
}
